train_dataset:
  noisy_path: "data/train/noisy"
  clean_path: "data/train/clean"
  duration: 3.0

valid_dataset:
  noisy_path: "data/val/noisy"
  clean_path: "data/val/clean"

dataloader:
  batch_size: 16
  num_workers: 4

optimizer:
  lr: 0.001

trainer:
  epochs: 100
  
save_path: "./checkpoints"